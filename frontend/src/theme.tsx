import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#2c7590',
    },
  },
  typography: {
    fontSize: 12,
  },
});

export default theme;
